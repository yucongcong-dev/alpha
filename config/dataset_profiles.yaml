# ============================================================================
# dataset_profiles.yaml — 数据集自适应运行参数
# ============================================================================
# 运行时根据 --dataset-id 自动匹配。
# 字段说明:
#   page_size                    字段 API 分页大小
#   min_request_interval          API 请求最小间隔 (秒)
#   sleep_between_fields          字段间休眠 (秒)
#   max_concurrent_simulations    最大并发模拟数
#   max_concurrent_creates        最大并发创建数
#   max_templates_per_field       每字段最大模板数 (0=全部)
#   field_template_batch_size     每轮每字段仅发出少量模板
#   simulation_max_wait_seconds   单个模拟最大等待 (秒)
#   simulation_max_queue_seconds  排队最大等待 (秒)
#   queue_busy_cooldown_seconds   队列繁忙冷却 (秒)
#   default_preset                未显式传入模板/过滤文件时使用的数据集 preset
#   paused                        禁止无显式研究入口的普通运行
# ============================================================================

dataset_profiles:
  analyst4:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 2
    max_concurrent_creates: 1
    max_templates_per_field: 2
    field_template_batch_size: 1
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
  fundamental2:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 2
    max_concurrent_creates: 1
    max_templates_per_field: 2
    field_template_batch_size: 1
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
  fundamental6:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 3
    max_concurrent_creates: 1
    max_templates_per_field: 6
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
  model16:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 3
    max_concurrent_creates: 1
    max_templates_per_field: 4
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
  model51:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 3
    max_concurrent_creates: 1
    max_templates_per_field: 4
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
  option8:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 3
    max_concurrent_creates: 1
    max_templates_per_field: 5
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
  news18:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 3
    max_concurrent_creates: 1
    max_templates_per_field: 2
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
  option9:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 3
    max_concurrent_creates: 1
    max_templates_per_field: 2
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
  socialmedia12:
    paused: true
    sleep_between_fields: 3.0
    max_concurrent_simulations: 3
    max_concurrent_creates: 1
    max_templates_per_field: 2
    simulation_max_wait_seconds: 900
    simulation_max_queue_seconds: 600
    queue_busy_cooldown_seconds: 60
